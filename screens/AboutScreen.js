import { ScrollView } from "react-native";
import { Card } from 'react-native-elements';

const AboutScreen = () => {
    return ( 
        <ScrollView>
            <Card/>
        </ScrollView>
     );
}
 
export default AboutScreen;